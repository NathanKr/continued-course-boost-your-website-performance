// --- do not change this

enum SaleCategory {
  regular = "reg",
  // maxDiscount = "discount-max", // 9.99$' not handled at first stage and may be never
  midDiscount = "discount-mid", // 12.99$'
}

export default SaleCategory;
